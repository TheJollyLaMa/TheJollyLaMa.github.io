app.controller('FoundMoundsController', ['$scope', function($scope) {

    // submit geolocation form

    // submit trip ticket form

    // submit story form


    // service factories to show connected wallet's interactions with global Archeological record

}]);
